# 12. SSE (Server-Sent Events)

**Status:** ⏳ Not Started

- Not implemented — there is no `src/sse/` module yet
- No real-time progress push to the frontend; the UI currently polls the list API and play endpoint (every 3s while work is in progress) to get updated status/progress

### TODO

- Implement SSE endpoint for real-time transcoding progress updates
- Push status changes (inspecting → transcoding → completed/failed) to connected clients
