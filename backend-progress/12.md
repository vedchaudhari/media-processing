# 12. SSE (Server-Sent Events)

**Status:** ⏳ Not Started

- `src/sse/` directory exists but is **empty**
- No real-time progress push to frontend yet — frontend would need to poll the list API or play endpoint to get updated status/progress

### TODO

- Implement SSE endpoint for real-time transcoding progress updates
- Push status changes (inspecting → transcoding → completed/failed) to connected clients
