# 20. Admin Seed Script — Credential Logging Fix

**Status:** ✅ Done

- **Removed a debug `console.log`** in `scripts/create-admin.ts` that printed the plaintext `ADMIN_PASSWORD` and the full `MONGO_URI` (which itself embeds DB credentials) to stdout on every `npm run create:admin` run — a leak into shell history, terminal scrollback, and any captured CI/deploy logs. No behavior change otherwise.

### Key Files

- `scripts/create-admin.ts`
